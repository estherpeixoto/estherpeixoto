```php
<?php

echo 'Sou a Esther, desenvolvedora fullstack, phpzeira convicta.';

// 🔧 Habilidades principais
$stack = [
    'Back-end' => ['PHP (Laravel, CodeIgniter)'],
    'Front-end' => ['JavaScript', 'TypeScript', 'React.js', 'Next.js', 'Vue.js'],
    'Mobile' => ['Flutter', 'React Native'],
    'Banco de dados' => ['MySQL', 'SQL Server', 'PostgreSQL'],
    'Dev Tools' => ['Git', 'Docker', 'Postman', 'Figma'],
    'Integrações' => ['REST', 'SOAP', 'SEFAZ', 'PIX', 'ERP TOTVS (Protheus)'],
];

// 🚧 Em desenvolvimento
$sideProject = [
    'nome' => 'SaaS para eventos geek/anime',
    'features' => [
        'multi-tenant',
        'venda de ingresso com Pix',
        'autenticação e controle de acesso baseado em RBAC',
        'painel administrador para cada sala temática',
        'inscrição de candidatos e sistema de avaliação para o concurso cosplay',
    ],
];

// 🧠 Loop existencial
function index()
{
    while (true) {
        codar();
        refatorar();
        beberCafe();
    }
}
```
